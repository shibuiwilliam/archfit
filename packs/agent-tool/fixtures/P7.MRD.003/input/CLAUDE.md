# Fixture: triggers P7.MRD.003

Has CLAUDE.md, Makefile, CHANGELOG.md, schemas/output.schema.json (with $id),
docs/exit-codes.md, and a cmd/ entrypoint — all the other rules are satisfied.
But there is no docs/adr/ directory, so P7.MRD.003 must fire.
