# Remediation: P2.SPC.004 — ADR missing YAML frontmatter

**Summary**: One or more ADR files in docs/adr/ lack YAML frontmatter.

## Decision tree

1. **Does the team use ADRs?**
   - No docs/adr/ directory → rule does not fire.
   - Yes → continue.

2. **Add frontmatter** to each ADR file:
   ```yaml
   ---
   id: "0001"
   title: Use PostgreSQL
   status: accepted
   date: 2026-01-15
   ---
   ```

3. **Re-scan** to confirm.

## See also

- `docs/rules/P2.SPC.004.md`
